import { createTheme } from "@mui/material";

const darktheme = createTheme({
  palette: {
    mode: "dark",
     primary: {
      main: "#237c2c",
    },
    secondary: {
      main: "#0a3610",
    },
  },
});

export default darktheme;
